# LLM providers
openai>=1.30.0
anthropic>=0.28.0

# Vector store
chromadb>=0.5.0

# UI
streamlit>=1.35.0

# Note: Streamlit Cloud deploys from the rag_assistant/ directory.
# Set root directory to rag_assistant/ in the Streamlit Cloud dashboard.
# Credentials are set via ~/.zshrc (local) or Streamlit Cloud secrets dashboard (prod).

